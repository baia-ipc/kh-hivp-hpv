params {
  conda_env = "${projectDir}/../pipelines/conda_env/pipeline.env.yml"
  scripts_dir = "${projectDir}/../scripts"
  multiqc_config = "${projectDir}/../pipelines/multiqc/pave_gene_mapping.multiqc.yml"
  bucket_tid = 151340
}

process {
  cpus = params.threads
}
